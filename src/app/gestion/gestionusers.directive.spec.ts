import { GestionusersDirective } from './gestionusers.directive';

describe('GestionusersDirective', () => {
  it('should create an instance', () => {
    const directive = new GestionusersDirective();
    expect(directive).toBeTruthy();
  });
});
